1) very brute force way is like we can get data is per as Data structure has been implemented like BST get data via traversal
if its linked list get via iterating.
2) its truly violating open closed principle